<div class="container py-5 d-flex justify-content-center align-items-center" style="min-height: 80vh;">
  <div class="card bg-white rounded-4 shadow-sm border-0 p-4" style="width: 100%; max-width: 600px;">
    <h4 class="mb-4 text-center">Log in</h4>

    <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="form-inputs">
        <%= f.input :email,
                    required: false,
                    autofocus: true,
                    input_html: { autocomplete: "email", class: "form-control" } %>

        <%= f.input :password,
                    required: false,
                    input_html: { autocomplete: "current-password", class: "form-control" } %>

        <%= f.input :remember_me, as: :boolean, wrapper_html: { class: "remember-me-wrapper" }, input_html: { class: "form-check-input square-checkbox me-1" }, label_html: { class: "form-check-label ms-2" } if devise_mapping.rememberable? %>
      </div>

      <div class="form-actions text-center mt-3">
        <%= f.button :submit, "Log in", class: "btn btn-primary rounded-pill px-4" %>
      </div>
    <% end %>

    <div class="text-center mt-3">
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
